dataset:
  dataset_name: "ArmisticeAI/OCTDL2024"
  num_classes: 5
  inchans: 3

training:
  batch_size: 32
  loss_fn: "cross-entropy-loss"
  optimizer: "adam"
  learning_rate: 0.0001
  num_epochs: 10

model:
  model_name: "convnext_tiny"

output:
  model_save_dir: "models"
  model_save_name: "convnext_tiny.pth"
  train_output_save_dir: "outputs/convnext_tiny-train"
  test_output_save_dir: "outputs/convnext_tiny-evaluation"
